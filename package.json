{
  "name": "health-app",
  "private": true,
  "version": "1.28.0",
  "description": "Health dashboard – web app and React/Capacitor Android build",
  "scripts": {
    "dev": "cd react-app && npm run dev",
    "build": "cd react-app && npm run copy-webapp && npm run build",
    "build:android": "npm run build && cd react-app && npx cap sync android",
    "android": "cd react-app && npx cap open android",
    "sync": "cd react-app && npx cap sync",
    "build:web": "node web/build-site.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "engines": {
    "node": ">=20"
  }
}

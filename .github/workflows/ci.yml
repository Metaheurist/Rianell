# =============================================================================
# Rianell - unified GitHub Actions
# =============================================================================
# Single workflow file replacing former: build-android-apk, build-ios,
# build-app-releases, deploy-pages, security-audit.
#
# Sections (jobs appear in dependency order):
#   1. paths-filter      - Push only: whether to run mobile build/release (skip if
#                          the only changed paths are under App build/, so bot
#                          commits do not rebuild APKs or duplicate releases).
#   2. android / ios     - PR: always. Push: only when paths-filter says so.
#   3. server-exe        - Build Windows server EXE artifacts (x64 + x86) with PyInstaller.
#   4. commit-app-build  - Push + mobile: commit App build/ to the branch.
#   5. publish-release   - Push + mobile: GitHub Release with Android + iOS + Server assets.
#   6. deploy-pages      - Push to main/master: site from web/ + App build/.
#   7. readme-build-info - After deploy (web-only runs): README beta table + build numbers.
#   8. security-audit    - Push / PR / workflow_dispatch: Gitleaks + npm + pip audits.
#
# Manual runs (workflow_dispatch): security audit only; mobile jobs are skipped.
# =============================================================================

name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

# JavaScript actions (checkout, setup-node, etc.) use a bundled Node runtime; GitHub is
# moving that default to Node 24. Opt in so we avoid Node 20 deprecation warnings.
# https://github.blog/changelog/2025-09-19-deprecation-of-node-20-on-github-actions-runners/
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # ---------------------------------------------------------------------------
  # Path gate for mobile release on push (replaces paths-ignore on old workflow)
  # ---------------------------------------------------------------------------
  paths-filter:
    name: Path gate (mobile release on push)
    runs-on: ubuntu-latest
    outputs:
      mobile_release: ${{ steps.out.outputs.mobile_release }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - id: out
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "mobile_release=false" >> $GITHUB_OUTPUT
          elif [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "mobile_release=false" >> $GITHUB_OUTPUT
          elif [ "${{ github.event_name }}" = "push" ]; then
            BEFORE="${{ github.event.before }}"
            SHA="${{ github.sha }}"
            if [ "$BEFORE" = "0000000000000000000000000000000000000000" ]; then
              echo "mobile_release=true" >> $GITHUB_OUTPUT
            else
              CHANGED=$(git diff --name-only "$BEFORE" "$SHA" 2>/dev/null || true)
              if [ -z "$CHANGED" ]; then
                echo "mobile_release=false" >> $GITHUB_OUTPUT
              elif echo "$CHANGED" | grep -qv '^App build/'; then
                echo "mobile_release=true" >> $GITHUB_OUTPUT
              else
                echo "mobile_release=false" >> $GITHUB_OUTPUT
              fi
            fi
          else
            echo "mobile_release=false" >> $GITHUB_OUTPUT
          fi

  # ---------------------------------------------------------------------------
  # Pre-minified web assets (temp artifacts used by all build flows)
  # ---------------------------------------------------------------------------
  prepare-minified-assets:
    name: Prepare minified temp assets
    needs: [paths-filter]
    if: |
      github.event_name == 'pull_request' ||
      github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm

      - name: Install root dependencies
        run: npm ci

      - name: Install React app dependencies
        working-directory: react-app
        run: npm ci

      - name: Build minified legacy bundle (temp)
        run: npm run build:web:apk

      - name: Build minified React shell (temp)
        run: npm run build:react

      - name: Prepare minified temp directories
        run: |
          rm -rf "ci-minified"
          mkdir -p "ci-minified"
          cp -r "web/.android-dist" "ci-minified/web-android-dist"
          cp -r "react-app/dist" "ci-minified/react-dist"

          mkdir -p "ci-minified/site"
          cp -r web/* "ci-minified/site/"
          if [ -d "App build" ]; then
            mkdir -p "ci-minified/site/App build"
            cp -r "App build/"* "ci-minified/site/App build/" 2>/dev/null || true
          fi
          node web/build-site.mjs --site "ci-minified/site"
          node -e "
          const fs = require('fs');
          let h = fs.readFileSync('ci-minified/site/index.html', 'utf8');
          h = h.replace(/app\\.js\\?v=(\\d+)/g, function (_, v) { return 'app.min.js?v=' + v; });
          fs.writeFileSync('ci-minified/site/index.html', h);
          "

          test -f "ci-minified/web-android-dist/app.min.js"
          test -f "ci-minified/react-dist/legacy/app.min.js"
          test -f "ci-minified/site/app.min.js"

      - name: Upload minified temp artifact
        uses: actions/upload-artifact@v5
        with:
          name: minified-prebuild
          path: ci-minified/

  # ---------------------------------------------------------------------------
  # Android APK (PR: CI; push: only when non–App-build paths changed)
  # ---------------------------------------------------------------------------
  android:
    name: Android APK
    needs: [paths-filter, prepare-minified-assets]
    if: |
      github.event_name == 'pull_request' ||
      (github.event_name == 'push' && needs.paths-filter.outputs.mobile_release == 'true')
    runs-on: ubuntu-latest
    concurrency:
      group: mobile-android-${{ github.ref }}-${{ github.event_name }}
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Sync branch with origin
        if: github.event_name == 'push'
        run: |
          git fetch origin "${{ github.ref_name }}"
          git checkout -B "${{ github.ref_name }}" "origin/${{ github.ref_name }}"

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm

      - name: Install root dependencies
        run: npm install

      - name: Install React app dependencies
        working-directory: react-app
        run: npm ci || npm install

      - name: Download minified temp artifact
        uses: actions/download-artifact@v5
        with:
          name: minified-prebuild
          path: ci-minified

      - name: Restore minified temp bundles (Android path)
        run: |
          rm -rf "web/.android-dist" "react-app/dist"
          cp -r "ci-minified/web-android-dist" "web/.android-dist"
          cp -r "ci-minified/react-dist" "react-app/dist"
          test -f "web/.android-dist/app.min.js"
          test -f "react-app/dist/legacy/app.min.js"

      - name: Add Android platform (if not present)
        working-directory: react-app
        run: npx cap add android || true

      - name: Prepare Android launcher source (1024× logo)
        run: node scripts/prepare-android-assets.mjs

      - name: Generate Android mipmap / adaptive icons & splash
        working-directory: react-app
        run: npx @capacitor/assets generate --android --iconBackgroundColor '#1a1d1e' --iconBackgroundColorDark '#0a0a0a' --splashBackgroundColor '#1a1d1e' --splashBackgroundColorDark '#0a0a0a'

      - name: Sync Capacitor Android
        working-directory: react-app
        run: npx cap sync android

      - name: Set Android SDK versions and build version
        working-directory: react-app
        env:
          BUILD_VERSION: ${{ github.run_number }}
        run: node patch-android-sdk.js

      - name: Parity gate (Android config + feature hooks)
        run: node scripts/check-platform-parity.mjs android

      - name: Setup Java 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"

      - name: Make gradlew executable
        working-directory: react-app/android
        run: chmod +x gradlew || true

      # Debug APK for sideload/beta; for smaller/faster runtime use local assembleRelease + signing (see docs/setup-and-usage.md).
      - name: Build debug APK
        working-directory: react-app/android
        run: ./gradlew assembleDebug --no-daemon

      - name: Output APK to App build/Android (versioned + latest)
        run: |
          rm -rf "App build/Android"
          mkdir -p "App build/Android"
          cp react-app/android/app/build/outputs/apk/debug/app-debug.apk "App build/Android/app-debug-beta.apk"
          cp react-app/android/app/build/outputs/apk/debug/app-debug.apk "App build/Android/app-debug-beta-${{ github.run_number }}.apk"
          echo '{"version":${{ github.run_number }},"file":"app-debug-beta-${{ github.run_number }}.apk"}' > "App build/Android/latest.json"

      - name: Upload APK artifact
        uses: actions/upload-artifact@v5
        with:
          name: android
          path: App build/Android/

      - name: Artifact note (PR)
        if: github.event_name == 'pull_request'
        run: |
          echo "Android artifact is available as 'android' on this run."

  # ---------------------------------------------------------------------------
  # iOS project + simulator zip (PR: CI; push: same gate as Android)
  # ---------------------------------------------------------------------------
  ios:
    name: iOS project + simulator
    needs: [paths-filter, prepare-minified-assets]
    if: |
      github.event_name == 'pull_request' ||
      (github.event_name == 'push' && needs.paths-filter.outputs.mobile_release == 'true')
    runs-on: macos-latest
    concurrency:
      group: mobile-ios-${{ github.ref }}-${{ github.event_name }}
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Sync branch with origin
        if: github.event_name == 'push'
        run: |
          git fetch origin "${{ github.ref_name }}"
          git checkout -B "${{ github.ref_name }}" "origin/${{ github.ref_name }}"

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm

      - name: Install root dependencies
        run: npm install

      - name: Install React app dependencies
        working-directory: react-app
        run: npm ci || npm install

      - name: Download minified temp artifact
        uses: actions/download-artifact@v5
        with:
          name: minified-prebuild
          path: ci-minified

      - name: Restore minified temp bundles (iOS path)
        run: |
          rm -rf "web/.android-dist" "react-app/dist"
          cp -r "ci-minified/web-android-dist" "web/.android-dist"
          cp -r "ci-minified/react-dist" "react-app/dist"
          test -f "web/.android-dist/app.min.js"
          test -f "react-app/dist/legacy/app.min.js"

      - name: Add iOS platform (if not present)
        working-directory: react-app
        run: |
          if [ ! -d ios ]; then
            npx cap add ios
          else
            echo "ios/ already present, skipping add"
          fi

      - name: Sync Capacitor iOS
        working-directory: react-app
        run: npx cap sync ios

      - name: Lock iOS to portrait (Info.plist)
        working-directory: react-app
        run: node patch-ios-orientation.js

      - name: Parity gate (iOS config + feature hooks)
        run: node scripts/check-platform-parity.mjs ios

      - name: Install CocoaPods dependencies
        working-directory: react-app/ios/App
        run: pod install || true

      - name: Reset App build/iOS for this run
        run: |
          rm -rf "App build/iOS"
          mkdir -p "App build/iOS"

      - name: Build for iOS Simulator (no signing required)
        working-directory: react-app
        run: |
          WS="ios/App/App.xcworkspace"
          if [ ! -d "$WS" ]; then echo "Workspace not found, skipping simulator build"; exit 0; fi
          if ! xcodebuild -workspace "$WS" -scheme App -configuration Debug \
            -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' \
            -derivedDataPath build clean build CODE_SIGN_IDENTITY="-" CODE_SIGNING_REQUIRED=NO 2>&1 | tee /tmp/xc.log | tail -30; then
            echo "Simulator build failed (project zip will still be produced); last lines:"
            tail -15 /tmp/xc.log
            exit 0
          fi
          APP_PATH="build/Build/Products/Debug-iphonesimulator/App.app"
          if [ -d "$APP_PATH" ]; then
            mkdir -p "../App build/iOS"
            RUN="${{ github.run_number }}"
            SIM_ZIP="Health-Tracker-simulator-alpha-${RUN}.zip"
            cd build/Build/Products/Debug-iphonesimulator
            zip -r "../../../../../App build/iOS/${SIM_ZIP}" App.app
            cd "../../../../.."
            echo "Simulator build: App build/iOS/${SIM_ZIP}"
          fi

      - name: Create App build/iOS and zip iOS project
        run: |
          mkdir -p "App build/iOS"
          RUN="${{ github.run_number }}"
          ZIPNAME="Health-Tracker-ios-alpha-build-${RUN}.zip"
          cd react-app
          zip -r "../App build/iOS/${ZIPNAME}" ios -x "*.DS_Store" -x "*DerivedData*" -x "*.xcuserstate"
          cd ..
          cp "App build/iOS/${ZIPNAME}" "App build/iOS/Health-Tracker-ios-alpha-latest.zip"
          SIM_ZIP="Health-Tracker-simulator-alpha-${RUN}.zip"
          if [ -f "App build/iOS/${SIM_ZIP}" ]; then
            echo "{\"version\":${RUN},\"file\":\"${ZIPNAME}\",\"simulatorFile\":\"${SIM_ZIP}\"}" > "App build/iOS/latest.json"
          else
            echo "{\"version\":${RUN},\"file\":\"${ZIPNAME}\"}" > "App build/iOS/latest.json"
          fi

      - name: Generate OTA manifest and installUrl (when .ipa present)
        env:
          RUN: ${{ github.run_number }}
          BASE_URL: ${{ vars.BUILD_IOS_BASE_URL }}
        run: |
          set -e
          IOS_DIR="App build/iOS"
          BASE="${BASE_URL:-https://rianell.com/}"
          BASE="${BASE%/}/"
          IPA_PATH=$(ls "$IOS_DIR"/*.ipa 2>/dev/null | head -n1) || true
          if [ -n "$IPA_PATH" ]; then
            IPA_FILE=$(basename "$IPA_PATH")
            IPA_URL="${BASE}App%20build/iOS/$(printf '%s' "$IPA_FILE" | sed 's/ /%20/g')"
            MANIFEST_URL="${BASE}App%20build/iOS/manifest.plist"
            cat > "$IOS_DIR/manifest.plist" << MANIFEST
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>items</key>
            <array>
              <dict>
                <key>assets</key>
                <array>
                  <dict>
                    <key>kind</key>
                    <string>software-package</string>
                    <key>url</key>
                    <string>${IPA_URL}</string>
                  </dict>
                </array>
                <key>metadata</key>
                <dict>
                  <key>bundle-identifier</key>
                  <string>com.healthapp.tracker</string>
                  <key>bundle-version</key>
                  <string>${RUN}</string>
                  <key>kind</key>
                  <string>software</string>
                  <key>title</key>
                  <string>Rianell</string>
                </dict>
              </dict>
            </array>
          </dict>
          </plist>
          MANIFEST
            INSTALL_URL="itms-services://?action=download-manifest&url=${MANIFEST_URL}"
            export INSTALL_URL
            node -e "
            const fs = require('fs');
            const j = JSON.parse(fs.readFileSync('$IOS_DIR/latest.json', 'utf8'));
            j.installUrl = process.env.INSTALL_URL;
            fs.writeFileSync('$IOS_DIR/latest.json', JSON.stringify(j));
            "
            echo "OTA installUrl added to latest.json"
          fi

      - name: Upload iOS artifact
        uses: actions/upload-artifact@v5
        with:
          name: ios
          path: App build/iOS/

      - name: Artifact note (PR)
        if: github.event_name == 'pull_request'
        run: |
          echo "iOS artifact is available as 'ios' on this run."

  # ---------------------------------------------------------------------------
  # Server Windows EXE (PyInstaller, x64 + x86)
  # ---------------------------------------------------------------------------
  server-exe:
    name: Server EXE (${{ matrix.arch }})
    needs: [paths-filter, prepare-minified-assets]
    if: |
      github.event_name == 'pull_request' ||
      (github.event_name == 'push' && needs.paths-filter.outputs.mobile_release == 'true')
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        arch: [x64, x86]
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          architecture: ${{ matrix.arch }}

      - name: Install server dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pyinstaller

      - name: Install UPX (best-effort)
        shell: pwsh
        run: |
          try {
            choco install upx -y --no-progress
          } catch {
            Write-Host "UPX install failed; continuing without UPX compression."
          }

      - name: Build server EXE
        shell: pwsh
        run: |
          $upxPath = (Get-Command upx -ErrorAction SilentlyContinue).Source
          $pyArgs = @(
            "--noconfirm",
            "--clean",
            "--onefile",
            "--optimize", "2",
            "--name", "rianell-server-${{ matrix.arch }}"
          )
          if ($upxPath) {
            $pyArgs += @("--upx-dir", (Split-Path -Parent $upxPath))
            Write-Host "Using UPX from: $upxPath"
          } else {
            Write-Host "UPX not found; building without UPX compression."
          }
          python -m PyInstaller @pyArgs "server/main.py"
          New-Item -ItemType Directory -Force -Path "App build/Server" | Out-Null
          Copy-Item "dist/rianell-server-${{ matrix.arch }}.exe" "App build/Server/rianell-server-${{ matrix.arch }}.exe" -Force
          Set-Content -Path "App build/Server/latest-${{ matrix.arch }}.json" -Value "{""version"":${{ github.run_number }},""file"":""rianell-server-${{ matrix.arch }}.exe""}"
          if ("${{ matrix.arch }}" -eq "x64") {
            Copy-Item "App build/Server/latest-${{ matrix.arch }}.json" "App build/Server/latest.json" -Force
          }

      - name: Upload server EXE artifact
        uses: actions/upload-artifact@v5
        with:
          name: server-exe-${{ matrix.arch }}
          path: App build/Server/

      - name: Artifact note (PR)
        if: github.event_name == 'pull_request'
        shell: pwsh
        run: |
          Write-Host "Server artifact is available as 'server-exe-${{ matrix.arch }}' on this run."

  # ---------------------------------------------------------------------------
  # Commit App build/ after successful mobile build (push + mobile only)
  # ---------------------------------------------------------------------------
  commit-app-build:
    name: Commit App build to repo
    needs: [paths-filter, android, ios, server-exe]
    if: |
      github.event_name == 'push' &&
      needs.paths-filter.outputs.mobile_release == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Sync branch with origin
        run: |
          git fetch origin "${{ github.ref_name }}"
          git checkout -B "${{ github.ref_name }}" "origin/${{ github.ref_name }}"

      - name: Clear App build dirs before artifacts (avoid stale checked-in files)
        run: |
          rm -rf "App build/Android" "App build/iOS" "App build/Server"
          mkdir -p "App build/Android" "App build/iOS" "App build/Server"

      - name: Download Android artifact
        uses: actions/download-artifact@v5
        with:
          name: android
          path: App build/Android

      - name: Download iOS artifact
        uses: actions/download-artifact@v5
        with:
          name: ios
          path: App build/iOS

      - name: Download Server EXE artifacts
        uses: actions/download-artifact@v5
        with:
          pattern: server-exe-*
          path: gh-server

      - name: Merge Server EXE artifacts
        run: |
          cp -f gh-server/server-exe-x64/rianell-server-x64.exe "App build/Server/" || true
          cp -f gh-server/server-exe-x86/rianell-server-x86.exe "App build/Server/" || true
          cp -f gh-server/server-exe-x64/latest-x64.json "App build/Server/" || true
          cp -f gh-server/server-exe-x86/latest-x86.json "App build/Server/" || true
          if [ -f gh-server/server-exe-x64/latest.json ]; then
            cp -f gh-server/server-exe-x64/latest.json "App build/Server/latest.json"
          else
            echo '{"version":${{ github.run_number }},"file":"rianell-server-x64.exe"}' > "App build/Server/latest.json"
          fi

      - name: Setup Node (README build-info script)
        uses: actions/setup-node@v5
        with:
          node-version: "22"

      - name: Write README beta build info
        env:
          GITHUB_RUN_NUMBER: ${{ github.run_number }}
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_RUN_ID: ${{ github.run_id }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: node scripts/update-readme-build-info.mjs

      - name: Commit and push App build
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md "App build/Android/" "App build/iOS/" "App build/Server/"
          git diff --staged --quiet || git commit -m "Update App build artifacts (build ${{ github.run_number }}) [skip ci]"
          git fetch origin "${{ github.ref_name }}"
          git push origin ${{ github.ref_name }}

  # ---------------------------------------------------------------------------
  # GitHub Release with Android + iOS binaries (push + mobile only)
  # ---------------------------------------------------------------------------
  publish-release:
    name: Publish GitHub Release
    needs: [paths-filter, android, ios, server-exe]
    if: |
      github.event_name == 'push' &&
      needs.paths-filter.outputs.mobile_release == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout (parity metadata)
        uses: actions/checkout@v5

      - name: Download Android binaries
        uses: actions/download-artifact@v5
        with:
          name: android
          path: gh-release/Android

      - name: Download iOS binaries
        uses: actions/download-artifact@v5
        with:
          name: ios
          path: gh-release/iOS

      - name: Download Server EXE binaries
        uses: actions/download-artifact@v5
        with:
          pattern: server-exe-*
          path: gh-release/Server

      - name: Prepare release assets (this run only)
        run: |
          set -e
          RUN="${{ github.run_number }}"
          mkdir -p release-assets/Android release-assets/iOS release-assets/Server release-assets/Meta
          cp "gh-release/Android/app-debug-beta.apk" "release-assets/Android/"
          cp "gh-release/Android/app-debug-beta-${RUN}.apk" "release-assets/Android/"
          # Distinct basenames: GitHub release assets cannot share the same filename.
          cp "gh-release/Android/latest.json" "release-assets/Android/android-latest.json"
          cp "gh-release/iOS/latest.json" "release-assets/iOS/ios-latest.json"
          cp "gh-release/iOS/Health-Tracker-ios-alpha-latest.zip" "release-assets/iOS/"
          cp "gh-release/iOS/Health-Tracker-ios-alpha-build-${RUN}.zip" "release-assets/iOS/"
          if [ -f "gh-release/iOS/Health-Tracker-simulator-alpha-${RUN}.zip" ]; then
            cp "gh-release/iOS/Health-Tracker-simulator-alpha-${RUN}.zip" "release-assets/iOS/"
          fi
          cp "gh-release/Server/server-exe-x64/rianell-server-x64.exe" "release-assets/Server/"
          cp "gh-release/Server/server-exe-x86/rianell-server-x86.exe" "release-assets/Server/"
          cp "gh-release/Server/server-exe-x64/latest-x64.json" "release-assets/Server/" || true
          cp "gh-release/Server/server-exe-x86/latest-x86.json" "release-assets/Server/" || true
          if [ -f "gh-release/Server/server-exe-x64/latest.json" ]; then
            cp "gh-release/Server/server-exe-x64/latest.json" "release-assets/Server/server-latest.json"
          fi
          cp "docs/platform-parity.json" "release-assets/Meta/platform-parity.json"
          echo "Release files:"
          find release-assets -type f | sort

      - name: Create GitHub Release with current build binaries
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ github.run_number }}
          name: Rianell build ${{ github.run_number }}
          body: |
            ## Build info
            - Build: **${{ github.run_number }}**
            - Branch: `${{ github.ref_name }}`
            - Commit: `${{ github.sha }}`

            ## Assets

            ### Android (Beta)
            Directory: `release-assets/Android/`
            - `app-debug-beta.apk` (rolling latest beta APK)
            - `app-debug-beta-${{ github.run_number }}.apk` (build-pinned APK)
            - `android-latest.json` (same metadata as `App build/Android/latest.json`)

            ### iOS (Alpha)
            Directory: `release-assets/iOS/`
            - `Health-Tracker-ios-alpha-latest.zip` (rolling latest iOS zip)
            - `Health-Tracker-ios-alpha-build-${{ github.run_number }}.zip` (build-pinned iOS zip)
            - `Health-Tracker-simulator-alpha-${{ github.run_number }}.zip` (when simulator artifact exists)
            - `ios-latest.json`

            ### Server (Windows EXE)
            Directory: `release-assets/Server/`
            - `rianell-server-x64.exe`
            - `rianell-server-x86.exe`
            - `server-latest.json`, `latest-x64.json`, `latest-x86.json` (metadata)

            ### Parity metadata
            Directory: `release-assets/Meta/`
            - `platform-parity.json` (machine-readable platform support matrix)

            ## Notes
            - This release is generated automatically by CI.
            - A workflow commit updates `App build/` on the branch so download links in the app/README stay current.
          draft: false
          prerelease: false
          fail_on_unmatched_files: true
          files: |
            release-assets/Android/*
            release-assets/iOS/*
            release-assets/Server/*
            release-assets/Meta/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ---------------------------------------------------------------------------
  # GitHub Pages - web/ + App build/ (every push to main/master)
  # ---------------------------------------------------------------------------
  deploy-pages:
    name: Deploy to GitHub Pages
    if: github.event_name == 'push'
    needs: [prepare-minified-assets]
    runs-on: ubuntu-latest
    environment: pages
    concurrency:
      group: pages
      cancel-in-progress: true
    permissions:
      contents: read
      pages: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Download minified temp artifact
        uses: actions/download-artifact@v5
        with:
          name: minified-prebuild
          path: ci-minified

      - name: Prepare site from minified temp artifact
        run: |
          rm -rf site
          cp -r "ci-minified/site" site
          test -f "site/app.min.js"

      - name: Inject Supabase config for GitHub Pages (from secrets)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
        run: |
          node -e "
          const fs = require('fs');
          const path = 'site/supabase-config.js';
          let c = fs.readFileSync(path, 'utf8');
          const url = process.env.SUPABASE_URL || '';
          const key = process.env.SUPABASE_ANON_KEY || '';
          // JSON.stringify so keys/URLs with quotes or backslashes cannot break the JS file (fixes SyntaxError on line ~19)
          c = c.replace(/url:\s*'https:\/\/YOUR_PROJECT_REF\.supabase\.co'/, 'url: ' + JSON.stringify(url || 'https://YOUR_PROJECT_REF.supabase.co'));
          c = c.replace(/anonKey:\s*'YOUR_SUPABASE_ANON_KEY'/, 'anonKey: ' + JSON.stringify(key || 'YOUR_SUPABASE_ANON_KEY'));
          fs.writeFileSync(path, c);
          "

      - name: Upload artifact (web app + App build as site root)
        uses: actions/upload-pages-artifact@v4
        with:
          path: site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ---------------------------------------------------------------------------
  # README beta table (push to main, web-only runs - mobile runs update README in commit-app-build)
  # ---------------------------------------------------------------------------
  readme-build-info:
    name: Update README beta build info
    needs: [deploy-pages, paths-filter]
    if: |
      success() &&
      github.event_name == 'push' &&
      (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') &&
      needs.paths-filter.outputs.mobile_release != 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Sync branch with origin
        run: |
          git fetch origin "${{ github.ref_name }}"
          git checkout -B "${{ github.ref_name }}" "origin/${{ github.ref_name }}"

      - uses: actions/setup-node@v5
        with:
          node-version: "22"

      - name: Write README beta build info
        env:
          GITHUB_RUN_NUMBER: ${{ github.run_number }}
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_RUN_ID: ${{ github.run_id }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: node scripts/update-readme-build-info.mjs

      - name: Commit and push README
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git diff --staged --quiet && { echo "No README changes"; exit 0; }
          git commit -m "docs(readme): update beta build info [skip ci]"
          git push origin "${{ github.ref_name }}"

  # ---------------------------------------------------------------------------
  # npm + pip dependency audits (push, PR, manual)
  # ---------------------------------------------------------------------------
  security-audit:
    name: Security audit
    if: |
      github.event_name == 'push' ||
      github.event_name == 'pull_request' ||
      github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Gitleaks (secret scan, working tree)
        run: |
          set -euo pipefail
          VERSION=8.28.0
          curl -sSfL "https://github.com/gitleaks/gitleaks/releases/download/v${VERSION}/gitleaks_${VERSION}_linux_x64.tar.gz" | tar -xz
          chmod +x gitleaks
          ./gitleaks detect --source . --config .gitleaks.toml --no-git --verbose --exit-code 1 --redact

      - name: Verify Supabase RLS baseline SQL (docs)
        run: node scripts/verify-rls-baseline.mjs

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: react-app/package-lock.json

      - name: npm audit (react-app)
        working-directory: react-app
        run: npm ci && npm audit --audit-level=high

      - name: npm audit (root / esbuild)
        run: npm install --no-save && npm audit --audit-level=high

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: pip-audit (requirements.txt)
        run: |
          python -m pip install --upgrade pip
          python -m pip install pip-audit
          pip-audit -r requirements.txt --desc on

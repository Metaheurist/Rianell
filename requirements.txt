# Health App Server - Python Dependencies
# Python 3.10+ recommended (3.8+ minimum). Install with: pip install -r requirements.txt
#
# pip resolves direct + peer constraints together. Re-run after upgrading supabase or
# pydantic manually so versions stay aligned (launch-server.ps1 does this automatically).

# Supabase client library (required for database operations)
supabase>=2.31.0

# Pydantic stack — keeps pydantic-core in sync with postgrest/supabase-auth (avoids SystemError on import)
pydantic>=2.10,<3

# Supabase realtime peer dependency (realtime 2.31.x requires websockets<16)
websockets>=11,<16

# File watching for auto-reload (optional but recommended)
watchdog>=3.0.0

# Environment variable management (required for .env file support)
# Floor >=1.2.2: OSV clean (GHSA-mf9w-mj56-hr94 fixed in 1.2.2).
python-dotenv>=1.2.2

# Cryptography for data encryption/decryption (required for encrypted anonymised data)
# Floor >=48.0.1: OSV clean (GHSA-537c-gmf6-5ccf fixed in 48.0.1; 46.x still flagged by OSV-Scanner).
cryptography>=48.0.1

# PostgreSQL drivers for direct SQL when DATABASE_URL is set (optional; at least one needed for exec_sql)
psycopg[binary]>=3.0.0
psycopg2-binary>=2.9.0

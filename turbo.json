{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["app.*.min.js", "asset-manifest.json", ".web-dist/**"]
    },
    "build:min": {
      "dependsOn": ["^build"],
      "outputs": ["app.*.min.js", "asset-manifest.json", ".web-dist/**"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "outputs": []
    }
  }
}

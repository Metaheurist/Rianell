# Withings connector

Imports weight, heart rate, and sleep metrics into daily logs.

- **OAuth scopes:** `user.metrics`, `user.activity`
- **API:** Withings `getmeas` / activity endpoints with `lastupdate` since `last_sync_at`
- **Setup:** [SETUP.md](./SETUP.md) §2

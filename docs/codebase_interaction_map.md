# Codebase Interaction Map

Canonical location moved to [`architecture/codebase-interaction-map.md`](architecture/codebase-interaction-map.md) (LocalRepoMapper / `.cursor/rules/codebase-mapping.mdc`).

Regenerate: ask an agent to generate a full interaction map of the app with `repo_map` into `docs/architecture/`.

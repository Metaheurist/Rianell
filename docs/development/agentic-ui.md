# Agentic AIO UI

Served at `/dev/agentic` from the Python server.

- Overview: brand + dry-run run-all CTA
- Run-all: 16-step timeline
- Per-pack tabs: model select (recommended default), start dry/live, pause/resume, report
- Models / Settings: catalog inspect

Tk dashboard Tools → **Agentic** opens the same URL.

# Copy to `.env` in this folder for local dev. Expo loads EXPO_PUBLIC_* at bundle time.
# Production CI can inject the same vars when building native binaries.

EXPO_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

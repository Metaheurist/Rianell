# Environment files (repo root - legacy)
.env
.env.local
.env.production

# security folder (preferred location for secrets)
security/.env
security/.env.local
security/.env.production
security/.encryption_key
security/*.key

# Local signing material (never commit)
.secrets/

# Encryption keys (repo root - legacy)
.encryption_key
encryption.key
*.key

# API keys and secrets
**/config/secrets.*
**/config/keys.*
replacements.txt

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
ENV/
env/
.venv

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Local library installations (repo root only; do not use bare `lib/` or benchmarks/scripts/lib is ignored)
/lib/

# Supabase local
.supabase/

# On-device LLM weight mirrors (hosted on Supabase Storage — do not commit)
apps/pwa-webapp/models/**
!apps/pwa-webapp/models/manifest.json
!apps/pwa-webapp/models/README.md

# Node modules
node_modules/

# Build outputs (root only — do not use bare build/ or scripts/build/ is ignored)
dist/
/build/
.firecrawl/
.wiki-sync/
# Cursor — commit team rules/skills; ignore local caches
.cursor/*
!.cursor/rules/
!.cursor/rules/**
!.cursor/skills/
!.cursor/skills/**
!.cursor/hooks.json
!.cursor/MCP-AND-CLI-GUIDE.md
ci-probe-test/
apps/pwa-webapp/app.min.js
.server-dist/
# Isolated dev Chromium (Playwright) for server/ debugging
server/.playwright-browsers/
server/.chromium-profiles/
server/*.bak
apps/pwa-webapp/app.*.min.js
apps/pwa-webapp/asset-manifest.json
apps/pwa-webapp/.trace-build/
apps/pwa-webapp/.web-dist/
ci-minified/
apps/pwa-webapp/styles.*.css

.aider*

backup/

# Memory stress test outputs (stress-test-memory.mjs)
benchmarks/memory/*.json

# Boot audit outputs (Phase 23)
audit-history/latest-boot-audit.json
audit-history/latest-boot-audit-previous.json
audit-history/axe-latest.json
audit-history/cwv-latest.json
audit-history/visual-baseline-manifest.json
!audit-history/baseline.json
!audit-history/phase-*.json

# Generated i18n audit output (regenerated by audit-hardcoded-strings.mjs)
scripts/.audit/*
!scripts/.audit/i18n-allowlist.json
!scripts/.audit/hardcoded-ui-baseline.json

# CI binaries → GitHub Releases only; repo keeps manifest JSON under artifacts/
artifacts/**/*.exe
artifacts/**/*.zip
